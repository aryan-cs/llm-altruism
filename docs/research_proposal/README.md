# Research Proposal

This folder contains the restored standalone research proposal source from the repository history.

Build the public upload PDF from this directory:

```bash
<tectonic> --outdir ../release research-proposal.tex
```

The upload PDF is `docs/release/research-proposal.pdf`. Its deposit metadata is in `docs/release/research-proposal-metadata.json`.
