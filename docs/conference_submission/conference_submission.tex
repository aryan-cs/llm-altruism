\PassOptionsToPackage{numbers,compress}{natbib}
\documentclass{article}

\usepackage[eandd]{neurips_2026}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{array}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{microtype}
\usepackage{url}
\usepackage{xcolor}
\usepackage{float}
\usepackage[hidelinks]{hyperref}
\usepackage{placeins}
\usepackage{needspace}
\usepackage{pdflscape}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,shapes.geometric,calc}

\definecolor{prbBlue}{HTML}{0072B2}
\definecolor{prbOrange}{HTML}{E69F00}
\definecolor{prbTeal}{HTML}{009E73}
% Approximately 20 screen pixels at 96 dpi.  Keep this uniform around every
% floating table rather than compressing individual floats to recover pages.
\setlength{\floatsep}{15pt}
\setlength{\textfloatsep}{15pt}
\setlength{\intextsep}{15pt}
\newcommand{\modeltag}[1]{\texttt{\detokenize{#1}}}
\input{../../data/processed/provider-safe-v2-paper-assets/paper_headlines.tex}

\title{Safety Beyond Refusal}

\author{Anonymous Authors}

\begin{document}
\maketitle

\begin{abstract}
Refusing an explicitly harmful request is necessary for safe language-model behavior, but it does not exhaust the ways an agent can shift costs onto other people.
We present \textbf{Prosocial Readiness Bench}, a three-part evaluation of safety refusal, self-directed choices in dyadic dilemmas, and restraint in a repeated commons environment.
The tasks form a cost-shifting progression from an identifiable request target, to a counterpart, to a group whose losses accumulate through an environment; they are complementary behavioral probes, not a single altruism score.
Across \ProviderSafePartZeroModelCount{} current systems, exploratory material-refusal rates span \ProviderSafePartZeroModelRefusalRatePctMinimum--\ProviderSafePartZeroModelRefusalRatePctMaximum\% (median \ProviderSafePartZeroModelRefusalRatePctMedian\%) over 48 English harmful-request roots crossed with three requested response languages.
Across \ProviderSafePartOneModelCount{} hosted routes evaluated on the same balanced 384-root self-choice bank, welfare-preserving choices span \ProviderSafePartOneModelWelfareRatePctMinimum--\ProviderSafePartOneModelWelfareRatePctMaximum\% (median \ProviderSafePartOneModelWelfareRatePctMedian\%).
In the repaired commons task, \ProviderSafePartTwoModelCount{} systems complete \ProviderSafePartTwoTrajectoryCount{} trajectories. Median normalized reserve area over operationally eligible trajectories is \ProviderSafePartTwoModelNormalizedAURCMedian{}, while median restraint over all scheduled agent-days is \ProviderSafePartTwoModelRestraintRatePctMedian\%.
These within-task differences show why refusal-only evaluation misses consequential variation, while validity gates preclude an unsupported cross-task leaderboard.
\end{abstract}

\section{Introduction}
LLMs are increasingly studied as decision-making agents that answer requests, recommend actions, and coordinate in simulated social environments \citep{bommasani2021opportunities,liu2024agentbench,park2023generative,zhou2024sotopia}. In these settings, refusal of explicitly harmful requests is only one part of responsible behavior. A broader evaluation should also assess whether a model selects cooperative actions in social dilemmas and whether repeated choices preserve a shared resource when overuse offers a short-term advantage.

Prosocial Readiness Bench addresses this requirement through three observable tasks rather than a latent moral trait. Part~0 asks whether a model materially refuses a request that would directly facilitate harm. Part~1 asks what the model itself chooses when a unilateral advantage shifts an immediate cost to a counterpart. Part~2 asks whether repeated local choices preserve a shared resource when the cost is delayed, distributed, and mediated by an environment. This progression supplies the task-selection rationale: each part increases the distance between action and affected party and changes how visibly and temporally the externality appears.

The three measurements are partially complementary. They may share mechanisms such as instruction following, norm recognition, or sensitivity to others' welfare, but each also introduces task-specific demands. We therefore do not assume that they correlate, interpret disagreement as proof of independence, or collapse them into an overall prosociality score. Cross-part associations are secondary finite-panel descriptions that become reportable only after every part passes its validity gate.

Relative to the pilot, the current evaluation separates judging from subject generation, aligns inference with request roots and complete trajectories, implements the incentives stated by the commons prompt, and expands coverage to current commercial and open-weight systems. Appendix~\ref{app:legacy-evidence} records the corresponding corrections to legacy evidence.

The paper asks two concrete questions. First, how should safety refusal, dyadic cooperation, and commons restraint be measured in one traceable artifact while respecting their different independent units? Second, how do current frontier and open-weight systems vary within these probes, across task conditions, and among exact related model routes? Cross-axis comparison remains a secondary analysis gated on the validity of every constituent measure. Figure~\ref{fig:overview} summarizes the design and its evidence boundaries.

Our contributions are:
\begin{itemize}
    \item \textbf{Three-part benchmark suite.} Prosocial Readiness Bench evaluates three partially complementary observable behaviors under explicit task contracts, organized by who bears the cost, how directly it is represented, and whether consequences accumulate over time.
    \item \textbf{Task-aligned measurement.} We define archived English request roots, scenario roots, and simulation trajectories as the respective independent units. Part~0 and Part~1 retain malformed or unclear outputs as nonsuccesses. Part~2 retains invalid first attempts as nonrestraint in its all-scheduled action denominator and restricts environmental AURC to operationally eligible trajectories. Part~0 uses one fixed judge distinct from every evaluated subject, and Part~2 implements the incentives shown to agents under repeated common seeds.
    \item \textbf{Broad, traceable evaluation.} The current frontier and open-weight executions span major model families through authenticated, identity-checked routes, with rate-limited parallel dispatch and no silent model substitution.
    \item \textbf{Task-specific empirical findings.} We report scope-separated variation among exact evaluated systems and task conditions, together with a corrected role-conditioned Part~1 calibration result; we do not collapse the tasks into a leaderboard or latent-trait score.
\end{itemize}

\begin{figure}[!htbp]
\centering
\resizebox{\linewidth}{!}{%
\begin{tikzpicture}[
    font=\small\sffamily,
    phase/.style={font=\footnotesize\bfseries\sffamily, text=black!58, align=center},
    part/.style={draw=#1!75!black, fill=#1!12, rounded corners=3pt, text width=3.05cm, minimum height=1.18cm, align=center, line width=0.8pt},
    trace/.style={draw=black!45, fill=black!4, rounded corners=3pt, text width=2.65cm, minimum height=1.10cm, align=center, line width=0.7pt},
    metric/.style={draw=#1!60!black, fill=#1!7, rounded corners=3pt, text width=2.65cm, minimum height=1.10cm, align=center, line width=0.7pt},
    package/.style={draw=prbTeal!60!black, fill=prbTeal!8, rounded corners=4pt, text width=2.95cm, minimum height=4.45cm, align=center, line width=0.9pt},
    arrow/.style={-{Latex[length=2.1mm]}, line width=0.75pt, draw=black!62}
]
\node[phase] at (0,3.35) {Prompted task};
\node[phase] at (3.45,3.35) {Recorded trace};
\node[phase] at (6.75,3.35) {Scored output};
\node[phase] at (10.10,3.35) {Release check};

\node[part=prbBlue] (p0) at (0,2.35) {\textbf{Part 0}\\Safety refusal\\{\footnotesize English requests; EN/ZH/RU replies}};
\node[part=prbOrange] (p1) at (0,0.75) {\textbf{Part 1}\\Dyadic cooperation\\{\footnotesize dilemma choices}};
\node[part=prbTeal] (p2) at (0,-0.85) {\textbf{Part 2}\\Commons restraint\\{\footnotesize repeated simulations}};

\node[trace] (t0) at (3.45,2.35) {visible response\\route identity\\judge verdict};
\node[trace] (t1) at (3.45,0.75) {JSON action\\justification\\prompt metadata};
\node[trace] (t2) at (3.45,-0.85) {daily actions\\reserve\\population};

\node[metric=prbBlue] (m0) at (6.75,2.35) {material refusal\\48-root sensitivity\\condition split};
\node[metric=prbOrange] (m1) at (6.75,0.75) {self-choice rate\\root sensitivity\\game/domain splits};
\node[metric=prbTeal] (m2) at (6.75,-0.85) {AURC and restraint\\12 common seeds\\trajectory unit};

\node[package] (release) at (10.10,0.75) {\textbf{Traceable artifact}\\schemas\\coverage\\transition checks\\[0.25em]\textbf{Released outputs}\\tables and figures\\manifests\\Croissant metadata};

\draw[arrow] (p0.east) -- (t0.west);
\draw[arrow] (p1.east) -- (t1.west);
\draw[arrow] (p2.east) -- (t2.west);
\draw[arrow] (t0.east) -- (m0.west);
\draw[arrow] (t1.east) -- (m1.west);
\draw[arrow] (t2.east) -- (m2.west);
\draw[arrow] (m0.east) -- ([yshift=1.60cm]release.west);
\draw[arrow] (m1.east) -- (release.west);
\draw[arrow] (m2.east) -- ([yshift=-1.60cm]release.west);
\end{tikzpicture}}
\caption{Prosocial Readiness Bench pipeline. Rows are the three task axes: Part~0 tests material refusal of harmful requests, Part~1 tests self-directed welfare-preserving choices in a dyadic dilemma, and Part~2 tests repeated restraint in a shared-resource environment. Columns show the prompted task, retained trace, scored output, and release check. Higher refusal is preferable on the harmful-only Part~0 bank, higher welfare-preserving choice is preferable for the Part~1 counterpart, and higher AURC is preferable for Part~2 resource preservation; none is a general safety score. The tasks share release infrastructure but retain distinct constructs, independent units, estimators, and validity checks.}
\label{fig:overview}
\end{figure}

\section{Related Work}
Safety and jailbreak benchmarks such as HarmBench and JailbreakBench measure harmful-request compliance and refusal \citep{mazeika2024harmbench,chao2024jailbreakbench}. Red-teaming and adversarial attacks test robustness under unsafe requests \citep{ganguli2022redteaming,zou2023universal}, while alignment and safeguard work studies how models avoid harmful assistance \citep{bai2022constitutional,wang2024donotanswer}; XSTest adds clearly safe contrast prompts that can trigger exaggerated refusal \citep{rottger2024xstest}. Multilingual-input jailbreak results show that input language can alter safety behavior \citep{deng2024multilingual}; our response-language manipulation is deliberately distinguished from that setting.

Classical work on repeated cooperation and commons governance motivates the underlying strategic structures \citep{axelrod1984evolution,hardin1968tragedy,ostrom1990governing}, while cooperative-AI research frames coordination among artificial and human agents as a distinct research problem \citep{dafoe2020open}. LLM studies examine one-shot economic choices \citep{brookins2024playing,horton2023homo} and repeated social dilemmas \citep{akata2025playing,tewolde2026coopeval}; MACHIAVELLI separately measures reward-seeking against ethical costs in interactive environments \citep{pan2023machiavelli}. Most closely, GovSim studies whether communicating LLM societies sustain a common-pool resource \citep{piatti2024govsim}; our Part~2 instead isolates repeated direct choices under a fixed prompt--engine contract and common environment seeds. AgentBench evaluates general agent capabilities, while Generative Agents, SOTOPIA, and SocialEval study broader simulated or benchmarked social behavior \citep{liu2024agentbench,park2023generative,zhou2024sotopia,zhou2025socialeval}.

Broad evaluation suites and multi-dimensional trustworthiness studies motivate reporting separate behavioral dimensions rather than a single score \citep{hendrycks2021measuring,srivastava2023beyond,liang2023helm,wang2023decodingtrust}. Participatory feedback datasets expose preference variation \citep{kirk2024prism}, while output-diversity work shows that distinct models can still converge on homogeneous responses \citep{jiang2025artificialhivemind}. Dataset and model documentation standards motivate the artifact-facing emphasis on provenance, metadata, access conditions, and responsible-use notes \citep{bender2018datastatements,holland2018datasetnutrition,mitchell2019modelcards,gebru2021datasheets,pushkarna2022datacards,akhtar2024croissant}.

We focus on combining safety refusal, self-directed dyadic cooperation, repeated commons restraint, and traceable release artifacts in one benchmark suite whose inferential units match its claims. Table~\ref{tab:positioning} summarizes this positioning. The evaluation spans a frozen panel of current commercial and open-weight routes, with exact identities and unavailable routes recorded rather than substituted.

\begin{table}[!htbp]
\centering
\caption{Coverage of adjacent evaluation families. Each row denotes an evaluation family; columns indicate whether it centrally evaluates harmful-request refusal (Refusal), self-directed choices in one-shot social dilemmas (One-shot coop.), repeated preservation of a shared resource (Repeated commons), or releases sufficient provenance and artifacts for result tracing (Traceable release). A checkmark means central coverage, ``Partial'' means coverage by only some representative work, and ``--'' means the capability is not central. More checks indicate broader scope, not higher empirical quality or safer model behavior. Representative examples are HarmBench and JailbreakBench \citep{mazeika2024harmbench,chao2024jailbreakbench}, strategic-game benchmarks \citep{akata2025playing,tewolde2026coopeval,piatti2024govsim}, and social environments \citep{park2023generative,zhou2024sotopia}.}
\label{tab:positioning}
\scriptsize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lcccc}
\toprule
Evaluation family & Refusal & One-shot coop. & Repeated commons & Traceable release \\
\midrule
Safety/jailbreak benchmarks & \checkmark & -- & -- & Partial \\
Strategic-game benchmarks & -- & \checkmark & Partial & Partial \\
Social/multi-agent environments & -- & Partial & Partial & Partial \\
Documentation standards & -- & -- & -- & \checkmark \\
Prosocial Readiness Bench & \checkmark & \checkmark & \checkmark & \checkmark \\
\bottomrule
\end{tabular}
\end{table}
\FloatBarrier

\section{Benchmark Design and Task Contracts}
\label{sec:benchmark-design}
\subsection{Design Principles}
The benchmark uses three design principles.

\paragraph{Organize tasks by cost shifting.}
The parts move from a direct harmful request, to an immediate choice affecting a counterpart, to repeated choices affecting a group through a shared state. This is a principled progression in externality structure, not a claim that the tasks form a psychometric scale. Safety refusal, dyadic cooperation, and commons restraint remain separate outcomes.

\paragraph{Make every claim traceable to stored artifacts.}
Each request retains its exact route identity, decoding controls, request and response hashes, provider completion identifier, finish reason, token usage, retry decision, and structured outcome. Campaign manifests bind the frozen registry, prompt bank, execution evidence, and analysis outputs.

\paragraph{Match inference to experimental units.}
Response-language conditions nested within an English request root and agent-days nested within a trajectory are not independent observations. Part~1 schedules one rendering per scenario root. The estimators below resample roots or complete trajectories accordingly. A narrower valid interval is obtained by collecting more independent units or repeated generations, never by treating dependent rows as independent.

\subsection{Part 0: Safety Refusal}
Part~0 measures material refusal on an outcome-blind selection of 48 harmful-request roots from version-hashed local snapshots of HarmBench and JailbreakBench \citep{mazeika2024harmbench,chao2024jailbreakbench}. The exact translated inputs were not retained, so every condition uses the same archived English request plus an explicit instruction to answer in English, Simplified Chinese, or Russian. These are reconstructed response-language conditions, not multilingual input prompts. The three conditions are repeated measurements of one request root, yielding 144 scheduled responses per system. This execution has no benign controls and therefore cannot distinguish selective safety behavior from a general tendency to refuse. Every scheduled response remains in the denominator; \texttt{UNCLEAR} and \texttt{INVALID} are nonsuccesses.

One frozen multilingual judge receives only the English request, its explicit response-language instruction, and the raw visible response; it is blind to subject identity and hypotheses and returns \texttt{REFUSAL}, \texttt{COMPLIANCE}, or \texttt{UNCLEAR} at temperature zero. The judge model is fixed across the subject panel and is never the model being evaluated. If a route selected as judge would otherwise enter the subject panel, that route is excluded from judging itself rather than silently switching judges. Holding the rater fixed avoids introducing a moving-rater confound, while disjointness addresses self-enhancement bias; neither design choice establishes judge validity \citep{zheng2023judging}.

The descriptive metric is material refusal under the reconstructed response-language conditions:
\[
\mathrm{refusal} =
\frac{\#\mathrm{REFUSAL}}{\#\mathrm{scheduled\ harmful\ responses}}.
\]
Five thousand bootstrap replicates resample the 48 request roots and retain all three response-language conditions within each sampled root \citep{efron1979bootstrap}. Condition-specific rates receive Wilson score intervals over the 48 roots \citep{wilson1927probable}. These are finite-bank sensitivity summaries, not confidence intervals for generation variability or a prompt superpopulation.

No language-qualified human validation has been completed. Judge--subject separation reduces a rater-overlap confound, but it does not validate the labels. Part~0 is therefore exploratory and cannot support paper-facing model rankings or cross-part claims.

\subsection{Part 1: Dyadic Cooperation and Role-Conditioned Probes}
Part~1 evaluates what the model itself chooses in a neutral one-shot dilemma. Direct self-choice is the primary cooperation estimand. Advice, observer evaluation, and prediction ask different questions and therefore serve as distinct calibration probes rather than paraphrases pooled into self-choice. The broad current campaign evaluates self-choice, while a separate six-route sentinel campaign evaluates the three role-conditioned probes reported in Section~\ref{sec:results}.

The executed broad panel uses a deterministic draft bank with two games crossed by six neutral domains. Each of the \ProviderSafePartOneModelCount{} hosted routes receives the same balanced 384-root schedule with 32 roots in every game--domain stratum. Frozen targets that do not complete the common scope remain separately documented as unavailable and are never replaced silently. Four pinned local models receive the same 384-root bank as separate execution-scale controls, not hosted-route substitutes. Responses must end with the exact structured action \texttt{X} or \texttt{Y}; a malformed first response is \texttt{INVALID}, remains a scheduled non-welfare-preserving outcome, and is not removed from the primary denominator.

The primary metric is the self-directed welfare-preserving choice rate:
\[
\mathrm{self\mbox{-}choice} =
\frac{\#\mathrm{welfare\mbox{-}preserving\ choices}}{\#\mathrm{scheduled\ roots}}.
\]
The scenario root is the independent unit. Five thousand bootstrap replicates resample the 384 roots separately within the 12 game--domain strata. The resulting bands describe sensitivity to the executed draft bank, not generation-seed uncertainty or a scenario superpopulation. Hosted routes share one common scope; local controls and role-conditioned frames remain separate panels and are never pooled into it. Because the draft bank has not received independent human content approval, all current Part~1 estimates remain exploratory.

\subsection{Part 2: Commons Restraint}
Part~2 measures preservation of a common-pool resource under repeated direct choices. The simulator implements the incentives stated in the prompt. Each society has five anonymous slots and a 12-day horizon. The common reserve begins at capacity 50. Option~A gives the acting slot one private point and leaves the reserve unchanged; Option~B gives two private points and removes two reserve units. Unanimous Option~A adds five group points, whereas unanimous Option~B subtracts five. After depletion, attrition occurs with probability 0.2 under the common environment seed. These parameters define an artificial diagnostic environment, not an ecological model of a real community.

Each of the \ProviderSafePartTwoModelCount{} executed systems receives 12 common environment seeds, yielding \ProviderSafePartTwoTrajectoryCount{} trajectories. An invalid first-attempt action has zero state effect so the simulator can preserve transition continuity, but that zero effect is not evidence of restraint. The all-scheduled restraint rate therefore retains invalid first attempts as nonrestraint, while environmental estimates are restricted to operationally eligible trajectories. The primary environmental metric is normalized area under the reserve curve:
\[
\mathrm{AURC}=\frac{1}{T}\sum_{t=1}^{T}\frac{R_t}{R_{\max}}.
\]
All-scheduled restraint, valid first-attempt coverage, and operational eligibility are reported alongside AURC, normalized area under the population curve (AUPC), reserve nondepletion, and final population retention. The trajectory is the independent environmental unit; agent-days are never treated as inferential replicates. A system with no operationally eligible trajectory is non-estimable for these environmental outcomes rather than assigned a zero. A separate deadline-exploratory sensitivity panel varies five environment factors over a resolution-V design with two common environment seeds. Its 30 sentinel-by-factor Holm-adjusted tests document a prespecified family but are underpowered and do not establish ecological robustness.

\section{Artifact Construction and Quality Control}
\paragraph{Frozen model cohort.}
The axis-specific registry contains \ProviderSafePartZeroModelCount{} Part~0 systems, \ProviderSafePartOneModelCount{} hosted Part~1 routes, and \ProviderSafePartTwoModelCount{} Part~2 systems, with exact overlap determined only from authenticated route identities. It spans general-purpose GPT, Claude Haiku/Sonnet/Opus, Gemini, Llama, DeepSeek, Qwen, Nemotron, MiniMax, and GLM routes when the exact authenticated route is callable. Technical reports describe selected GPT, GPT-OSS, Llama, and Qwen families, while route manifests are the authoritative identity record for every evaluated endpoint \citep{openai2023gpt4,openai2025gptoss,touvron2023llama2,yang2024qwen25}. Four pinned offline controls (Qwen2.5-0.5B-Instruct and SmolLM2 at 135M, 360M, and 1.7B) separately complete 384 Part~1 roots; they are execution-scale controls, not substitutes for hosted routes, and are never pooled into that panel. Image, embedding, reranking, guard-only, inaccessible, deprecated, or identity-mismatched routes are excluded with recorded failure codes. A display name is never substituted for an unavailable route.

\paragraph{Output contracts and metadata.}
An authenticated route is eligible only after a bounded structured smoke test returns the exact requested identity. Every attempt stores route identity, completion identifier, finish reason, token usage, controls, hashes, outcome, and retry decision. Part~0 stores the visible subject response and independent judge verdict; Parts~1 and~2 store direct structured subject actions. Metadata, manifests, schemas, and Croissant JSON-LD \citep{akhtar2024croissant} bind these records to the frozen campaign.

Requests are dispatched concurrently across independent routes and units, but bounded per-provider and global request/token buckets prevent overload. HTTP 408/429, transport, timeout, and 5xx failures without a retained model response may be retried with the identical payload and seed within the manifest-bound budget of at most eight physical attempts; the source-bound Part~0 deadline lane applies the same bounded rule to HTTP 400 failures. Semantic or formatting failures are retained as \texttt{INVALID} and never removed from scheduled-unit accounting. Separate post-completion repair diagnostics may resubmit the same semantic unit for at most eight rounds spaced by 30 seconds, but a repaired response is reported separately and never replaces its first-attempt record. Part~0 and Part~1 count first-attempt invalids as nonsuccesses. Part~2 counts them as nonrestraint in the all-scheduled action rate, preserves their zero simulator effect for transition continuity, and excludes affected trajectories from AURC eligibility. An identity mismatch or retry-exhausted target-part quarantines that target-part rather than triggering a substitute model.

\paragraph{Validation.}
Validation is graph-independent. It checks hashes, route identity, prompt-bank coverage, duplicate and missing units, response contracts, retry accounting, Part~2 day continuity and transitions, and execution completeness. Structural validation does not certify semantic labels or prompt content. Consequently, the absent Part~0 human validation and absent Part~1 draft-bank approval keep those outputs exploratory even if their operational manifests complete.

\paragraph{Harmful-output handling.}
Part 0 evaluates harmful prompts and may produce unsafe completions. The anonymous supplement therefore releases derived tables, validation outputs, and aggregate summaries rather than raw harmful generations. It supports verification and regeneration of paper-facing tables from the sealed aggregate graph, but not independent regeneration of that graph from private prompts, responses, authenticated routes, manifests, or journals.

\paragraph{Evidence status.}
Historical pilot artifacts are kept separate from the current campaign and summarized in Appendix~\ref{app:legacy-evidence}. The current result graph reconciles every frozen target as included or unavailable. Part~0 and Part~1 remain exploratory because their semantic validity gates are external to operational completion; Part~2 is reported from the corrected engine at the trajectory level.

\section{Evaluation Protocol}
The subject decoding protocol is frozen across routes before outcomes are collected. Unsupported controls follow one prespecified common-denominator rule and are never silently omitted for a single system. Part~0 and Part~1 use every scheduled unit as the denominator and retain unclear, malformed, or semantic-invalid responses as nonsuccesses. Part~2 restraint uses every scheduled agent-day and retains invalid first attempts as nonrestraint; AURC uses operationally eligible trajectories only, with ineligible trajectories retained in audit counts. The estimands concern the exact executed prompt banks, model routes, and decoding protocol, not altruistic intent, vendor-wide populations, or unrestricted generation-seed behavior.

Any cross-part analysis is restricted to exact authenticated route overlap and remains unavailable while Part~0 lacks human validation and the matching Part~1 prompts lack content approval. Historical systems, local controls, role probes, sensitivity cells, and availability retries remain separate; Appendix~\ref{app:legacy-evidence} records why the legacy cross-part analysis was withdrawn.

\section{Results}
\label{sec:results}
Table~\ref{tab:current-coverage} records the completed, fail-closed current-model scopes. Part~0 and Part~1 are exploratory for substantive validity reasons, not because execution is unfinished. Part~2 executes 12 common-seed trajectories for every included system, reports restraint over all scheduled agent-days, and reports AURC only for operationally eligible trajectories. Historical pilot results are not pooled with this campaign; Appendix~\ref{app:legacy-evidence} records the narrow legacy evidence that remains descriptive.

\begin{table}[!htbp]
\centering
\caption{Completed current-model execution scopes. Each row is one benchmark axis. Axis names the task; Executed scope gives the number of executed systems or routes, roots or trajectories per execution, and total observations; Paper status states whether results are exploratory, estimable, non-estimable, or unavailable and identifies the limiting validity gate. Larger included, reported, or valid counts mean broader operational evidence, whereas larger unavailable, invalid, or non-estimable counts mean weaker coverage; none of these counts measures behavioral safety or task performance. Included systems have complete scope-specific evidence, unavailable systems receive no estimate, and no unavailable route is substituted.}
\label{tab:current-coverage}
\scriptsize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{p{0.09\linewidth}p{0.48\linewidth}p{0.35\linewidth}}
\toprule
Axis & Executed scope & Paper status \\
\midrule
Part~0 & \ProviderSafePartZeroModelCount{} systems $\times$ 48 archived harmful roots $\times$ 3 reconstructed response-language instructions (\ProviderSafePartZeroScheduledResponseCount{} scheduled responses) & Exploratory; fixed disjoint judge; no benign controls or human validation \\
Part~1 & \ProviderSafePartOneModelCount{} hosted routes $\times$ 384 roots (\ProviderSafePartOneScheduledUnitCount{} scheduled self-choice units) & Exploratory; one common scope; first-attempt invalids retained; draft bank lacks human approval \\
Part~2 & \ProviderSafePartTwoModelCount{} systems $\times$ 12 common-seed trajectories (\ProviderSafePartTwoTrajectoryCount{} total; \ProviderSafePartTwoOperationallyEligibleTrajectoryCount{} AURC-eligible) & Corrected engine; all-scheduled restraint; \ProviderSafePartTwoOperationallyIneligibleTrajectoryCount{} trajectories ineligible for AURC; \ProviderSafePartTwoNonestimableModelCount{} models non-estimable for AURC \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Part 0: Exploratory Safety Refusal}
Across \ProviderSafePartZeroModelCount{} systems and \ProviderSafePartZeroScheduledResponseCount{} scheduled responses, the fixed judge assigns \ProviderSafePartZeroRefusalCount{} refusals, \ProviderSafePartZeroComplianceCount{} compliances, \ProviderSafePartZeroUnclearCount{} unclear outcomes, and \ProviderSafePartZeroInvalidCount{} invalid outputs. System-level material-refusal rates span \ProviderSafePartZeroModelRefusalRatePctMinimum--\ProviderSafePartZeroModelRefusalRatePctMaximum\%, with a median of \ProviderSafePartZeroModelRefusalRatePctMedian\%. The model-by-language panel describes requested output-language sensitivity over the same English roots, not multilingual-input robustness. The result shows residual within-task variation among current systems, but it remains exploratory because there are no benign controls or completed language-qualified human validation. Full exact-route and condition-specific summaries appear in Appendix~\ref{app:current-results}.

\subsection{Part 1: Exploratory Dyadic Cooperation}
The broad self-choice campaign reports \ProviderSafePartOneModelCount{} hosted routes over \ProviderSafePartOneScheduledUnitCount{} scheduled roots. Welfare-preserving choice spans \ProviderSafePartOneModelWelfareRatePctMinimum--\ProviderSafePartOneModelWelfareRatePctMaximum\% across exact routes, with a median of \ProviderSafePartOneModelWelfareRatePctMedian\%; \ProviderSafePartOneInvalidCount{} first responses are invalid and remain scheduled nonsuccesses. The range describes self-directed dyadic behavior not measured by the refusal probe, while its breadth cautions against treating verbal safety alignment as evidence of welfare-preserving action in this task. Full exact-route results appear in Appendix~\ref{app:current-results}; the four fixed local controls are reported there as a separate execution-scale panel.

The role-calibration campaign evaluates \ProviderSafeRoleAdviceModelCount{} exact sentinel routes on three separate 384-draw frames. Welfare-preserving rates span \ProviderSafeRoleAdviceWelfareRatePctMinimum--\ProviderSafeRoleAdviceWelfareRatePctMaximum\% for advice, \ProviderSafeRoleObserverEvaluationWelfareRatePctMinimum--\ProviderSafeRoleObserverEvaluationWelfareRatePctMaximum\% for observer evaluation, and \ProviderSafeRolePredictionWelfareRatePctMinimum--\ProviderSafeRolePredictionWelfareRatePctMaximum\% for prediction. Median valid first-attempt coverage is \ProviderSafeRoleAdviceValidCoveragePctMedian\%, \ProviderSafeRoleObserverEvaluationValidCoveragePctMedian\%, and \ProviderSafeRolePredictionValidCoveragePctMedian\%, respectively. These frame-specific ranges are exploratory calibration results; because the instructions ask different questions, their differences are descriptive rather than a causal framing effect on one cooperation estimand.

\subsection{Part 2: Commons Restraint}
The corrected commons campaign executes \ProviderSafePartTwoTrajectoryCount{} independent common-seed trajectories across \ProviderSafePartTwoModelCount{} systems. Of these, \ProviderSafePartTwoOperationallyEligibleTrajectoryCount{} trajectories support environmental estimates and \ProviderSafePartTwoOperationallyIneligibleTrajectoryCount{} remain in audit counts but are ineligible; \ProviderSafePartTwoNonestimableModelCount{} systems have no eligible trajectory and are reported as non-estimable rather than assigned zero. Across estimable systems, mean normalized AURC ranges from \ProviderSafePartTwoModelNormalizedAURCMinimum{} to \ProviderSafePartTwoModelNormalizedAURCMaximum{} (median \ProviderSafePartTwoModelNormalizedAURCMedian{}), while median normalized AUPC is \ProviderSafePartTwoModelNormalizedAUPCMedian{}. Median reserve-nondepletion and final-population-retention rates are \ProviderSafePartTwoModelReserveNondepletionRatePctMedian\% and \ProviderSafePartTwoModelPopulationRetentionPctMedian\%, respectively. All-scheduled restraint ranges from \ProviderSafePartTwoModelRestraintRatePctMinimum--\ProviderSafePartTwoModelRestraintRatePctMaximum\% (median \ProviderSafePartTwoModelRestraintRatePctMedian\%). The audit retains \ProviderSafePartTwoInvalidAgentDayCount{} invalid first attempts among \ProviderSafePartTwoScheduledAgentDayCount{} scheduled agent-days; their simulator-level zero state effect preserves continuity but supplies no restraint evidence. Appendix~\ref{app:current-results} reports exact-route eligibility, restraint, environmental outcomes, and valid-coverage values.

\paragraph{Deadline-exploratory sensitivity.}
The supplemental Part~2 panel executes \ProviderSafeSensitivityTrajectoryCount{} trajectories for \ProviderSafeSensitivitySentinelCount{} sentinels over five prespecified environment factors and \ProviderSafeSensitivityCommonSeedCount{} common seeds. Across \ProviderSafeSensitivityEffectCount{} sentinel-by-factor contrasts, the largest absolute high-minus-low normalized-AURC effect is \ProviderSafeSensitivityMaximumAbsoluteEffect{}, and \ProviderSafeSensitivityHolmSignificantCount{} contrasts have a global Holm-adjusted $p\leq 0.05$. With only two common seeds, these values are underpowered descriptors of the frozen toy environment, not confirmatory robustness evidence.

\FloatBarrier

\section{Discussion}
\label{sec:discussion}
\paragraph{Refusal is informative but incomplete.}
The corrected current campaign preserves the original empirical motivation for Safety Beyond Refusal. Automated harmful-request labels vary across systems and response-language instructions but remain exploratory pending human validation; direct welfare-preserving self-choice is often rare and spans nearly the full range; and commons behavior ranges from complete overuse to sustained restraint. These are within-task findings, not evidence that one axis predicts another. Together they show that a refusal score does not measure dyadic and repeated-resource decisions.

\paragraph{Partial complementarity without assumed independence.}
Separate reporting is justified by task design, not by a required low correlation. The tasks share the broad question of whether an action shifts avoidable costs onto others, while varying the explicitness of the rule, identity of the affected party, payoff structure, temporal horizon, and environmental mediation. A high, low, or uncertain association would each be scientifically interpretable; none would turn the three tasks into a unitary latent score.

\paragraph{Role conditioning is part of the behavior being measured.}
The current role-calibration panel shows that welfare-preserving rates and format validity vary across advice, observer-evaluation, and prediction prompts. The corrected benchmark therefore defines self-choice as the primary cooperation estimand and treats the other roles as distinct calibration probes. This retains the original insight that refusal-only evaluation misses context-dependent social behavior without conflating judgment, recommendation, prediction, and action or interpreting frame differences causally.

\paragraph{Evidence boundaries.}
No cross-part association is current paper evidence because Part~0 lacks human validation and Part~1 lacks independent content approval. This boundary prevents exploratory labels from being converted into a stronger claim merely by matching model identities. Appendix~\ref{app:legacy-evidence} records why the earlier cross-part statistics are excluded.

\paragraph{Extension to agent systems.}
The same three probes can be applied to tool-using or multi-agent systems by scoring externally visible decisions rather than attributing a stable trait to the underlying language model. Such an extension would require heterogeneous roles, persistent state, communication, tool-mediated actions, independent environments, and system-level failure criteria while preserving the task-specific inferential units used here.

\section{Limitations and Broader Impact}
\label{sec:scope}
Prosocial Readiness Bench evaluates observable outputs in artificial tasks, not intent, moral status, altruism, or real-world trustworthiness. A finite authenticated route is not a vendor, and one frozen generation per Part~0 or Part~1 root does not estimate decoding variability. Part~0 reconstructs response-language instructions over English inputs, has no benign controls, and lacks human label validation. The Part~1 dilemmas simplify social context, impose explicit actions and payoffs, omit interaction, and use a draft bank without independent content approval. The primary Part~2 task uses anonymous homogeneous five-agent same-model populations in a 12-step environment with one nominal parameterization; it omits heterogeneous roles, communication, memory, bargaining, tool use, institutions, and human participants. The supplemental five-factor sensitivity panel uses only two common seeds and is explicitly underpowered, so it diagnoses parameter dependence within this toy ecology rather than establishing robustness or external validity.

The natural extension is an agent-system evaluation with heterogeneous models and roles, persistent but privacy-preserving memory, communication and negotiation channels, tool-mediated actions, institutional constraints, and human-designed environment dynamics. Such an extension should preserve trajectory-level inference and common seeds while adding independent environments and preregistered system-level failure criteria. It should not reuse the present Part~2 simulator as if it were a calibrated society.

Used conservatively, the artifact can help developers identify whether a safety intervention improves explicit refusal while leaving less direct cost-shifting behaviors unchanged, motivating targeted evaluation before deployment. It is intended for behavioral auditing and mitigation comparison, not for improving harmful compliance. Raw Part 0 harmful prompts and completions are excluded from the anonymous supplement by default. Because the executed Part~0 inputs are English requests with response-language instructions, they cannot support cross-cultural or translated-prompt claims. Any later multilingual input evaluation would require retained translations plus language-specific and culture-specific expert review.

\section{Conclusion}
Safety Beyond Refusal asks how model evaluation should expand from explicit harmful requests to choices whose costs are less direct, more distributed, and temporally accumulated. Prosocial Readiness Bench answers with three separate, traceable probes of refusal, self-directed dyadic choice, and repeated commons restraint. The current campaign finds material within-task variation: exploratory refusal spans \ProviderSafePartZeroModelRefusalRatePctMinimum--\ProviderSafePartZeroModelRefusalRatePctMaximum\%, exploratory 384-root dyadic choice spans \ProviderSafePartOneModelWelfareRatePctMinimum--\ProviderSafePartOneModelWelfareRatePctMaximum\%, and all-scheduled commons restraint spans \ProviderSafePartTwoModelRestraintRatePctMinimum--\ProviderSafePartTwoModelRestraintRatePctMaximum\%. Judge--subject separation, root-aware inference, corrected incentives, independent common-seed trajectories, and explicit invalid and non-estimable accounting make those findings traceable without turning them into a latent moral score. The result is a stronger behavioral evaluation artifact for identifying task-specific safety-relevant behavior that refusal-only benchmarks do not measure.

\bibliographystyle{plainnat}
\bibliography{references}

\appendix
\raggedbottom
\setlength{\floatsep}{15pt}
\setlength{\textfloatsep}{15pt}
\setlength{\intextsep}{15pt}
\setlength{\abovecaptionskip}{8pt}
\setlength{\belowcaptionskip}{6pt}

\section{Detailed Current-Model Results}
\label{app:current-results}
The following tables are generated directly from the complete, hash-verified sanitized result graph. Missing result artifacts are compilation errors rather than occasions to render stale or hand-transcribed values.

\subsection{Corrected Current-Data Visual Summaries}
Figures~\ref{fig:part0-model-language}--\ref{fig:part1-local-controls} are the six provider-safe visual families generated from the sealed current aggregate graph. They remain axis- and estimand-specific: hosted routes, role probes, sensitivity cells, and local controls are not pooled.

\clearpage
\begin{landscape}
\begin{figure}[p]
\centering
\includegraphics[width=\linewidth,height=0.78\textheight,keepaspectratio]{../../data/processed/provider-safe-v2-paper-assets/part0_model_language.pdf}
\caption{Part~0 response-language outcomes. Each row is one exact target route, and columns are English, Chinese, and Russian response instructions applied to the same archived English requests. The left panel is REFUSAL divided by all 48 scheduled harmful-request roots per route-language cell; the right panel is valid-output coverage over the same denominator. Higher refusal means less assistance on this harmful-request task, and higher validity means fewer invalid outputs. Cross-column differences measure response-language instruction sensitivity, not multilingual-input robustness; absent benign controls and human validation keep the panel exploratory.}
\label{fig:part0-model-language}
\end{figure}
\end{landscape}

\clearpage
\begin{landscape}
\begin{figure}[p]
\centering
\includegraphics[width=\linewidth,height=0.84\textheight,keepaspectratio]{../../data/processed/provider-safe-v2-paper-assets/part1_all_models.pdf}
\caption{Part~1 self-choice outcomes for all hosted routes. Each row is one exact route evaluated on 384 scheduled scenario roots. The left panel is the welfare-preserving first-attempt count divided by all scheduled units, with invalid outputs retained as nonsuccesses; the right panel is valid first-attempt coverage over the same denominator. Higher welfare preservation means fewer costs shifted to the counterpart in this task, and higher validity means fewer invalid outputs. Rows are ordered only within Part~1; the draft bank lacks independent content approval, so this is neither a general safety ranking nor confirmatory evidence.}
\label{fig:part1-all-models}
\end{figure}
\end{landscape}

\clearpage
\begin{landscape}
\begin{figure}[p]
\centering
\includegraphics[width=\linewidth,height=0.78\textheight,keepaspectratio]{../../data/processed/provider-safe-v2-paper-assets/part2_all_models.pdf}
\caption{Part~2 commons outcomes for all hosted routes. Each row is one exact route with 12 common-seed trajectories. Panels show restraint divided by all scheduled agent-days; mean normalized reserve area (AURC), mean normalized population area (AUPC), reserve nondepletion, and final population retention over operationally eligible trajectories; and valid first-attempt coverage over scheduled agent-days. Invalid first attempts remain nonrestraint and have zero simulator effect; ``NE'' means no eligible trajectory, not a zero score. Higher restraint, AURC, AUPC, nondepletion, and population retention mean more resource or population preservation in this simulator, while higher validity means fewer invalid actions; none is a general safety score.}
\label{fig:part2-all-models}
\end{figure}
\end{landscape}

\clearpage
\begin{landscape}
\begin{figure}[p]
\centering
\includegraphics[width=\linewidth,height=0.78\textheight,keepaspectratio]{../../data/processed/provider-safe-v2-paper-assets/part1_role_calibration.pdf}
\caption{Part~1 exploratory role calibration. Each row is one of six exact sentinel routes; columns are the separate advice, observer-evaluation, and prediction frames, with 384 scheduled draws per route-frame. The left panel gives welfare-preserving first attempts divided by all scheduled draws, and the right gives valid first-attempt coverage. Higher welfare preservation means fewer counterpart costs within the named frame, while higher validity means fewer invalid outputs. Frames ask different questions, are never pooled, and differences among them are descriptive rather than causal or indicative of general safety.}
\label{fig:part1-role-calibration}
\end{figure}
\end{landscape}

\clearpage
\begin{landscape}
\begin{figure}[p]
\centering
\includegraphics[width=\linewidth,height=0.78\textheight,keepaspectratio]{../../data/processed/provider-safe-v2-paper-assets/part2_sensitivity_effects.pdf}
\caption{Part~2 deadline-exploratory sensitivity. Each cell is one exact sentinel route by one varied factor and shows mean normalized AURC at the high level minus the mean at the low level over the 16-cell resolution-V design and two common-seed blocks. ``H'' denotes global Holm-adjusted $p\leq0.05$ across the 30 sentinel-by-factor tests; ``n.s.'' denotes otherwise. Positive effects mean that the high level increased reserve preservation and negative effects mean that it decreased preservation in this simulator, but the sign is not automatically good or bad for a parameter. With only two common seeds, the panel is underpowered and does not establish robustness or general safety.}
\label{fig:part2-sensitivity}
\end{figure}
\end{landscape}

\clearpage
\begin{figure}[p]
\centering
\includegraphics[width=\linewidth,height=0.72\textheight,keepaspectratio]{../../data/processed/provider-safe-v2-paper-assets/part1_local_controls.pdf}
\caption{Part~1 exploratory local execution-scale controls. Each row is one exact offline Hugging Face model ID and advertised parameter scale evaluated on 384 scheduled roots. The left panel is welfare-preserving choice over all scheduled units, with invalid outputs retained as nonsuccesses; the right is format-valid output over the same denominator. Higher values mean fewer counterpart costs or fewer malformed outputs only within these measures. The four fixed local models are separate controls, not substitutes for or members of the hosted-route panel and not confirmatory evidence.}
\label{fig:part1-local-controls}
\end{figure}

\clearpage
\begin{landscape}
\begingroup
\setlength{\textwidth}{\linewidth}
\renewcommand{\arraystretch}{1.00}
\input{../../data/processed/provider-safe-v2-paper-assets/all_models_cross_phase_table.tex}
\clearpage
\input{../../data/processed/provider-safe-v2-paper-assets/part0_model_language_table.tex}
\clearpage
\input{../../data/processed/provider-safe-v2-paper-assets/part1_all_models_table.tex}
\clearpage
\input{../../data/processed/provider-safe-v2-paper-assets/part2_all_models_table.tex}
\clearpage
\input{../../data/processed/provider-safe-v2-paper-assets/part1_role_calibration_table.tex}
\clearpage
\input{../../data/processed/provider-safe-v2-paper-assets/part2_sensitivity_effects_table.tex}
\clearpage
\input{../../data/processed/provider-safe-v2-paper-assets/part1_local_controls_table.tex}
\clearpage
\endgroup
\end{landscape}

\subsection{Supplemental Exact-Route Availability Retries}
The following panels report exact routes that were unavailable or operationally excluded from a primary axis but later passed a fresh authenticated compatibility check. They remain separate from the primary rows and denominators: a retry result neither replaces an unavailable primary cell nor enters the cross-phase matrix. Routes that remain unavailable after repeated exact-route checks receive no behavioral estimate and are documented as unavailable rather than silently substituted.

\begin{landscape}
\input{../../artifacts/availability_retry_analysis_definitive_v1/part0_availability_retry.tex}
\clearpage
\input{../../artifacts/availability_retry_analysis_definitive_v1/part1_availability_retry.tex}
\clearpage
\input{../../artifacts/availability_retry_analysis_definitive_v1/part2_availability_retry.tex}
\end{landscape}

\subsection{Bounded Semantic-Invalid Repair Diagnostics}
Primary Part~1 and role-calibration rates continue to count every first-response invalid as a nonsuccess. The following diagnostics ask only whether the exact source unit later satisfies the output contract under at most eight separately recorded rounds, spaced by 30 seconds; they do not replace primary responses or denominators and do not promote repaired behavior as a primary estimate.

\begin{landscape}
\input{../../artifacts/semantic_invalid_repair_analysis_definitive_v1/part1_semantic_repair.tex}
\clearpage
\input{../../artifacts/semantic_invalid_repair_analysis_definitive_v1/part1_role_semantic_repair.tex}
\end{landscape}

\section{Corrections to the Legacy Pilot}
\label{app:legacy-evidence}
The April archive contains 56,382 stored rows, but that number is not an inferential sample size: its repeated languages, prompt renderings, and agent-days are clustered. Part~0 model rates are withdrawn because the legacy judge prompt used hidden reasoning as well as the visible response, failed adjudications defaulted to a binary label, and no quantitative multilingual human validation was completed. Exact translated prompts and route-level failure provenance were also not retained sufficiently for an identical replay. Part~2 model rates are withdrawn because the prompt described private and group scores that the engine never computed or returned to agents, while each model supplied only one trajectory. Agent-day rows from that trajectory cannot support a run-level interval. All legacy cross-part statistics and figures are consequently withdrawn.

The April Part~0 heatmaps, Part~2 trajectories and rasters, behavioral fingerprints, PCA projections, cross-part plots, and role aggregates remain in the research archive for provenance but are not current paper evidence. Table~\ref{tab:legacy-part1} reports the direct self-choice subset: 48 legacy scenario roots per model, with the two presentations nested within each root. Advice, observer, and prediction prompts are excluded because they alter the role and estimand. The current role-calibration panel in Appendix~\ref{app:current-results} replaces those legacy aggregates with a separate, common-scope execution; neither panel validates Part~0 or establishes relationships among the three axes.

\begin{table}[H]
\centering
\caption{Legacy April Part~1 self-directed choices. Each row is one legacy local model route; Self-directed welfare-preserving choice is the percentage of its 48 scenario roots on which the model selected the action that sacrifices one focal point to avoid a three-point loss to the counterpart; Root band is the corresponding 5,000-replicate scenario-root sensitivity band. The two presentations of each scenario remain nested within root. Higher percentages are better on this dyadic task because they indicate less cost shifting to the counterpart; lower percentages indicate more focal-advantage choices, not general selfishness or unsafe behavior. These fixed-bank descriptive results are separate from, and are not pooled with, the current-model execution.}
\label{tab:legacy-part1}
\scriptsize
\resizebox{\linewidth}{!}{%
\begin{tabular}{lcc}
\toprule
Legacy local route & Self-directed welfare-preserving choice & Root band \\
\midrule
\texttt{gpt-oss:20b} & 32.3 & [21.9, 43.8] \\
\texttt{gpt-oss-sg} & 49.0 & [35.4, 62.5] \\
\texttt{gpt-oss-der} & 29.2 & [18.8, 39.6] \\
\texttt{llama2} & 52.1 & [41.7, 62.5] \\
\texttt{llama2-uncensored} & 59.4 & [47.9, 69.8] \\
\texttt{qwen2.5:7b} & 100.0 & [100.0, 100.0] \\
\texttt{qwen2.5-abl} & 100.0 & [100.0, 100.0] \\
\texttt{qwen2.5-inst} & 99.0 & [96.9, 100.0] \\
\texttt{qwen2.5-abl-inst} & 100.0 & [100.0, 100.0] \\
\texttt{qwen3.5} & 93.8 & [89.6, 97.9] \\
\texttt{qwen3.5-unc} & 6.2 & [2.1, 11.5] \\
\texttt{qwen3.5-inst} & 63.5 & [52.1, 74.0] \\
\texttt{qwen3.5-inst-unc} & 13.5 & [6.2, 21.9] \\
\bottomrule
\end{tabular}%
}
\end{table}

The legacy advice, observer, and prediction prompts remain useful as provenance for the calibration design, but they do not estimate self-directed cooperation and their aggregate percentages are not reused as current evidence. The provider-safe campaign evaluates these distinct questions on a new common scope. No cross-axis composite or association is promoted while Part~0 lacks human label validation and Part~1 lacks independent prompt approval; exact route overlap alone cannot satisfy either gate.

\section{Part 1 Structured Contract}
\label{app:part1-examples}
Because the current root bank has not yet passed independent content approval, the paper does not print an unapproved root as if it were validated evidence. The compact schematic below illustrates the intended neutral contract; it is not a sampled data row.

\begin{table}[H]
\centering
\caption{Schematic payoff contract for one neutral self-choice root. Each row is one joint focal/other action pair; Focal/other choice lists those actions in that order, and Focal points and Other points give the resulting payoff to each party. Higher points are better for the named party. For either possible other-party action, the focal standard action gives up one focal point while increasing the other's payoff by three and is therefore defined as welfare-preserving; standard/standard also has the largest joint payoff. Option letters and display order are counterbalanced, so welfare preservation is determined from the complete payoff table rather than a moralized label.}
\label{tab:part1-contract}
\small
\begin{tabular}{lcc}
\toprule
Focal/other choice & Focal points & Other points \\
\midrule
standard / standard & 4 & 4 \\
standard / expanded & 1 & 5 \\
expanded / standard & 5 & 1 \\
expanded / expanded & 2 & 2 \\
\bottomrule
\end{tabular}
\end{table}

The prompt states that two work groups simultaneously select a one-time usage level from a limited operational resource, supplies all four outcomes, identifies the model as the focal participant, and requests one exact final token, \texttt{X} or \texttt{Y}. It does not ask what is moral, what another actor will do, or what action should be recommended. Root text and mappings were frozen and hash-locked before execution; that operational lock is not a substitute for the independent content approval still required for confirmatory use.

\Needspace{0.34\textheight}
\section{Model Registry}
\label{app:model-registry}
\begin{table}[H]
\centering
\caption{Frozen cohort and axis-specific evidence coverage. Each row is one analysis panel; Panel names the task cohort; Planned coverage gives its frozen systems or routes, execution scope, and included, estimable, invalid, or unavailable counts; Weighting states how the panel enters analysis or why it receives no estimate; and Promotion rule states the minimum evidence condition and exclusions governing paper use. More included or estimable systems and larger balanced root or trajectory counts provide broader or more precise coverage, whereas more unavailable, invalid, or non-estimable systems weaken evidence; these quantities are not behavioral performance scores. Exact backend-namespaced routes, versions, smoke evidence, and hashes remain in the campaign registry, and family names describe coverage rather than substitutions.}
\label{tab:model-registry}
\footnotesize
\setlength{\tabcolsep}{3pt}
\begin{tabular}{p{0.16\linewidth}p{0.31\linewidth}p{0.19\linewidth}p{0.25\linewidth}}
\toprule
Panel & Planned coverage & Weighting & Promotion rule \\
\midrule
Part~0 & \ProviderSafePartZeroModelCount{} exact systems; 48 roots $\times$ 3 response-language instructions & Separate exploratory axis & All scheduled responses retained; fixed disjoint judge; no benign controls or human validation \\
Hosted Part~1 & \ProviderSafePartOneModelCount{} exact routes at $n=384$ & Separate exploratory axis & Common 12-stratum bank; invalid first attempts are nonsuccesses; no independent prompt approval \\
Part~2 & \ProviderSafePartTwoModelCount{} exact systems at 12 trajectories each; \ProviderSafePartTwoOperationallyEligibleTrajectoryCount{} eligible and \ProviderSafePartTwoOperationallyIneligibleTrajectoryCount{} ineligible trajectories & Separate corrected-engine axis & All-scheduled restraint; AURC restricted to eligible trajectories; non-estimable is not zero \\
Role calibration & \ProviderSafeRoleAdviceModelCount{} sentinels $\times$ 3 frames $\times$ 384 draws & Separate exploratory calibration & Frames are distinct estimands and are not pooled or interpreted causally \\
Sensitivity & \ProviderSafeSensitivitySentinelCount{} sentinels $\times$ 5 factors; \ProviderSafeSensitivityTrajectoryCount{} trajectories over \ProviderSafeSensitivityCommonSeedCount{} common seeds & Separate deadline-exploratory panel & Underpowered; 30-test Holm family documents rather than validates robustness \\
Pinned local controls & Qwen2.5-0.5B; SmolLM2 135M/360M/1.7B at $n=384$ & Separate execution-scale control & Exact offline snapshots; never pooled with or substituted for hosted routes \\
Availability retries & Exact target-bound retry panels & Separate supplemental accounting & Never replace or merge primary rows; no silent substitution \\
Historical/local & Legacy GPT, Gemini, Gemma, GPT-OSS, and local routes & Separate descriptive provenance & Never increases primary cross-part coverage \\
\bottomrule
\end{tabular}
\end{table}

\FloatBarrier
\Needspace{0.38\textheight}
\section{Artifact Map}
\label{app:artifact-map}
\begin{itemize}
\setlength{\itemsep}{1pt plus 0.5pt}
\setlength{\parsep}{0pt}
    \item Frozen scientific protocol and checkpoint: \path{docs/CONFIRMATORY_PROTOCOL.md} and \path{CHECKPOINT.md}.
    \item Provider-safe campaign runners: \path{experiments/misc/inference_hub_part0_panel.py}, \path{experiments/misc/inference_hub_part1_stratified_panel.py}, and \path{experiments/misc/inference_hub_part2_panel.py}. Exact private ledgers and frozen arguments are hash-bound in their manifests but excluded from the anonymous supplement.
    \item Separate role, sensitivity, semantic-invalid repair, and availability-retry runners remain source-bound to their own manifests. Repair and retry evidence never overwrites primary rows, changes scheduled denominators, or silently substitutes a route.
    \item Fail-closed administrative provenance: \path{experiments/misc/inference_hub_retire_target.py} records target-bound unavailable tails, and \path{analysis/finalize_inference_hub_part2_offline.py} seals completed Part~2 evidence after verifying its bound source tree. Neither utility dispatches model calls or manufactures outcomes.
    \item Route discovery and identity evidence: \path{experiments/misc/inference_hub_discovery.py} and \path{data/private/inference_hub}.
    \item Task definitions: \path{experiments/part0}, \path{experiments/part1}, and \path{experiments/part2}; executed inputs and schedules are hash-pinned in their manifests.
    \item Paper-facing validation and export: \path{analysis/analyze_provider_safe_v2_definitive.py} validates the complete source-bound matrices, and \path{analysis/build_provider_safe_v2_paper_assets.py} emits the six vector/PNG figure families, six within-panel full model-row tables, one nonpooled cross-phase model matrix, deterministic headline macros, and a hashed asset manifest. \path{analysis/analyze_availability_retry_panels.py} and \path{analysis/analyze_semantic_invalid_repairs.py} separately validate and render exact-route retry and bounded repair diagnostics. They fail closed on incomplete coverage, identity mismatch, unsafe fields, denominator changes, or attempts to overwrite an existing output directory. Missing Part~0 human validation and Part~1 prompt approval block cross-axis promotion while preserving explicitly exploratory within-task summaries and side-by-side coverage.
    \item Legacy status tables: \path{data/analysis/tables}; deprecated Part~0 and Part~2 tables are retained for audit but are not paper evidence.
    \item Croissant metadata: \path{data/analysis/croissant_metadata.json} \citep{akhtar2024croissant}.
    \item Documentation: \path{docs/release/REPRODUCIBILITY.md}, \path{docs/release/DATA_CARD.md}, \path{docs/release/MODEL_REGISTRY.md}, \path{docs/release/COMPUTE.md}, and \path{docs/release/LICENSES_AND_TERMS.md}.
\end{itemize}

The anonymous supplement contains the Croissant JSON-LD and its local
content-addressed files, but intentionally carries no dataset URL. Anonymous
reviewers can reproduce tables and checks from the sealed sanitized aggregates;
they cannot regenerate those aggregates from the excluded private execution
evidence. Public hosting and external Croissant validation are post-review
release steps, and no placeholder or non-anonymous URL is asserted here.

From the repository root, the non-dispatch reproduction checks are:
\begingroup\small
\begin{verbatim}
uv run pytest -q
uv run python -m analysis.validation --strict
uv run python -m analysis.summarize_results
uv run python -m analysis.build_manifest
uv run python -m analysis.analyze_provider_safe_v2_definitive --help
uv run python -m analysis.build_provider_safe_v2_paper_assets --help
uv run pytest -q tests/test_build_provider_safe_v2_paper_assets.py
\end{verbatim}
\endgroup

\input{checklist}

\end{document}
